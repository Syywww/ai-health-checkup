<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.health.member.mapper.MemberMapper">

    <resultMap type="com.health.member.domain.Member" id="MemberResult">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="gender" column="gender"/>
        <result property="birthday" column="birthday"/>
        <result property="phone" column="phone"/>
        <result property="idCard" column="id_card"/>
        <result property="address" column="address"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectMemberVo">
        select id, name, gender, birthday, phone, id_card, address, status, create_by, create_time, update_by, update_time, remark
        from member
    </sql>

    <select id="selectMemberList" parameterType="Member" resultMap="MemberResult">
        <include refid="selectMemberVo"/>
        <where>
            <if test="name != null and name != ''"> and name like concat('%', #{name}, '%')</if>
            <if test="phone != null and phone != ''"> and phone = #{phone}</if>
            <if test="idCard != null and idCard != ''"> and id_card = #{idCard}</if>
            <if test="status != null and status != ''"> and status = #{status}</if>
        </where>
        order by create_time desc
    </select>

    <select id="selectMemberById" parameterType="Long" resultMap="MemberResult">
        <include refid="selectMemberVo"/> where id = #{id}
    </select>

    <insert id="insertMember" parameterType="Member" useGeneratedKeys="true" keyProperty="id">
        insert into member (
        <if test="name != null and name != ''">name,</if>
        <if test="gender != null and gender != ''">gender,</if>
        <if test="birthday != null">birthday,</if>
        <if test="phone != null and phone != ''">phone,</if>
        <if test="idCard != null and idCard != ''">id_card,</if>
        <if test="address != null and address != ''">address,</if>
        <if test="status != null and status != ''">status,</if>
        <if test="remark != null and remark != ''">remark,</if>
        create_by, create_time
        ) values (
        <if test="name != null and name != ''">#{name},</if>
        <if test="gender != null and gender != ''">#{gender},</if>
        <if test="birthday != null">#{birthday},</if>
        <if test="phone != null and phone != ''">#{phone},</if>
        <if test="idCard != null and idCard != ''">#{idCard},</if>
        <if test="address != null and address != ''">#{address},</if>
        <if test="status != null and status != ''">#{status},</if>
        <if test="remark != null and remark != ''">#{remark},</if>
        #{createBy}, sysdate()
        )
    </insert>

    <update id="updateMember" parameterType="Member">
        update member
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="gender != null and gender != ''">gender = #{gender},</if>
            <if test="birthday != null">birthday = #{birthday},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="idCard != null and idCard != ''">id_card = #{idCard},</if>
            <if test="address != null and address != ''">address = #{address},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="remark != null and remark != ''">remark = #{remark},</if>
            update_by = #{updateBy}, update_time = sysdate()
        </set>
        where id = #{id}
    </update>

    <delete id="deleteMemberById" parameterType="Long">
        delete from member where id = #{id}
    </delete>

    <delete id="deleteMemberByIds" parameterType="Long">
        delete from member where id in
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="countMemberByDate" resultType="int">
        select count(*) from member
        <where>
            <if test="startDate != null and startDate != ''">and date_format(create_time,'%Y-%m-%d') &gt;= #{startDate}</if>
            <if test="endDate != null and endDate != ''">and date_format(create_time,'%Y-%m-%d') &lt;= #{endDate}</if>
        </where>
    </select>

</mapper>